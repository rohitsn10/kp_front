import { Dialog, DialogContent } from '@mui/material'
import React from 'react'

function ProjectUpdate({open,handleClose}) {

  return (
    <Dialog
        open={open}
        onClose={handleClose}
    >
        <DialogContent>

        </DialogContent>
    </Dialog>
  )
}

export default ProjectUpdate